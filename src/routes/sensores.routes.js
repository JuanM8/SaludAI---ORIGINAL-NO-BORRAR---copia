/*const express = require("express");
const router = express.Router();
//const sensorController = require('../controllers/sensores.controller');
const sensoresCtrl = require("../controllers/sensores.controller");

router.post('/sensores', sensoresCtrl.createSensor);
/*router.get('/:id', sensorController.showSensor);
router.put('/:id', sensorController.updateSensor);
router.delete('/:id', sensorController.deleteSensor);

module.exports = router;*/
