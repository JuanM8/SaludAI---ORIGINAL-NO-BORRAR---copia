<!--div class="wrapper">
  <!-- Content Wrapper. Contains page content -->
  <div class="content-wrapper">
    <div class="content-header">
      <div class="container">
        <div class="row mb-2">
          <div class="col-sm-6">
            <h1 class="m-0 text-dark"> Bienvenido a las Gráficas <small></small></h1>
            <p>En SaludAI, estamos comprometidos a proporcionarte los datos y sus graficas con la mayor precisión posible.</p>
            <form action="/enviar-datos" method="POST">
              <!-- Contenido del formulario (por ejemplo, campos de entrada, botones, etc.) -->
              <button type="submit" class="btn btn-success">Frecuencia cardiaca</button>
            </form>
            <!-- Crear grafico -->
            <div>
              <canvas id="grafico" width="2000" height="2000"></canvas>
            </div>

            <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

            <script>

              const temperaturaData = {{temperaturaN}} 
              console.log(temperaturaData);

              const alturaData = {{alturaN}} 
              console.log(alturaData);

              const pesoData = {{pesoN}} 
              console.log(pesoData);

              const ultimos10Valores = temperaturaData.slice(-10); // Obtén solo los últimos 10 valores

                // Obtén el contexto del gráfico
                const ctx = document.getElementById('grafico').getContext('2d');

                // Crea el gráfico de línea para los datos de temperatura
                new Chart(ctx, {
                  type: 'bar',
                  data: {
                  labels: ultimos10Valores.map((_, index) => index + 1), // Etiquetas del eje X para los últimos 50 valores
                    datasets: [{
                      label: 'Temperatura (°C)',
                      data: temperaturaData,
                      borderColor: 'rgba(255, 99, 132, 1)', // Color de la línea del gráfico
                      borderWidth: 3
                    },
                    {
                      label: 'Altura (cm)',
                      data: alturaData,
                      borderColor: 'rgba(75, 192, 192, 1)',
                      borderWidth: 3
                    },
                    {
                      label: 'Peso (Kg)',
                      data: pesoData,
                      borderColor: 'rgba(181, 39, 245, 0.8)',
                      borderWidth: 3
                    },
                    ]
                  },
                  options: {
                    scales: {
                      y: {
                        beginAtZero: true
                      }
                    }
                  }
                });

            </script>          
          </div>
        </div><!-- /.row -->
      </div><!-- /.container-fluid -->
    </div>
    <div class="container">
      <div class="row">
        <div class="col">
        </div>
      </div>
    </div>
  </div><!-- /.content-wrapper -->
</div--><!-- /.wrapper -->